const register = async (req, res) => {
  // get all the values
  // check whether all the values are present
  // check if user exists with the same name or email
  // create the user account
  // generate the accessToken and refreshToken from the user
  // return the response and save the accessToken and refresh token in cookies
  res.send("ok");
};

export { register };
